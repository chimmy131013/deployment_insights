services:

  backend:
    build: ./backend
    container_name: deployment-insights-backend
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: deployment-insights-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend